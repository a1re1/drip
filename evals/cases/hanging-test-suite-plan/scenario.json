{
  "version": 1,
  "goal": "The test suite hangs: python3 -m unittest discover -s tests -q never returns. Find the cause and fix tests/test_server.py so the suite passes without hanging",
  "candidates": [
    "build-feature",
    "exact-commands",
    "explain-repo",
    "finish-in-progress",
    "fix-bug",
    "fix-review-findings",
    "investigate-report",
    "port-module",
    "remove-feature",
    "scaffold-project",
    "ship-pr",
    "sweep-rename",
    "verify-only"
  ]
}
