__pycache__/
# Per-run detail: regenerated by every run and large (the full 1312-query
# sample is megabytes). The small append-only results/history.jsonl is the
# tracked record of what a run scored.
results/latest-*.json
results/answers/
results/judgments/
